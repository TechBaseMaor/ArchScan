# ArchScan Frontend — local development environment
# Copy to .env:  cp .env.example .env
# NEVER commit real secrets. Only VITE_* public vars belong here.

# Backend API URL (localhost for dev, https://archscan.onrender.com for prod)
VITE_API_BASE_URL=http://localhost:8000
