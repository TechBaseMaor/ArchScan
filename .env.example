# ArchScan Backend — local development environment
# Copy to .env:  cp .env.example .env
# NEVER commit real secrets to this file or to .env.

# Postgres connection (leave empty for local JSON-file storage)
DATABASE_URL=

# Comma-separated frontend origins allowed for CORS
# (localhost defaults are always included; add production domains here)
ALLOWED_ORIGINS=
